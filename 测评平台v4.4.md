PRD v4.4 修订文档 · Agent 自动接入
本文档为 v4.3 的增量修订,只改"接入机制"这一块:把"平台用死规则模拟 Agent(gen-script/run-script/fix-script + 标记 + 非空判定 + 3次上限)"整体替换为"真 Agent + tool use"。v4.3 其余内容(三标签、5 板块、评价、跑批、ComfyUI、数据存储等)全部沿用。SCHEMA_VERSION 升至 6。

〇、本次修订核心理念
为什么改
v4.3 用"平台写死规则"去模拟一个 Agent(生成脚本→跑→报错喂回改→3 次上限→标记解析→非空判成功),这套笨且脆弱:AI 输出截断就崩、修复死板、判断成败靠死规则。真实体验远不如 Claude Code 那种"读文档→写脚本→自测→自己装库→反复迭代到跑通"的 Agent。

怎么改
把上述死规则整体换成真 Agent + tool use(function calling):

平台后端跑一个 Agent 循环,给 AI 配几个工具(跑脚本 / 装库 / 保存接口)。AI 自主驱动整个接入过程——自己写脚本、自己决定跑、自己看报错、自己装库、自己反复改,直到真跑通,再自己整理参数和能力标签、保存接口。平台只提供工具和循环,不再用死规则去管"先干啥后干啥/成没成功"。

关键定调
干活的 Agent 模型固定为 DeepSeek(V4 Pro / deepseek-chat,需支持 function calling),由用户接入其 API Key,不可换成不支持 tool use 的模型。
被测评的目标模型仍可自由接入/选择(它们只被对比,不碰工具)。
v4.3 里的 /api/gen-script、/api/run-script(作为独立测试入口)、/api/fix-script、SCRIPT/META 标记、修复 3 次上限——全部废弃,由 Agent 取代。
脚本本身的约定(stdin 传参、RESULT_JSON 结果格式、非空判成功、不许编造数据)保留,但不再由平台代码强制,而是写进 Agent 的 system prompt,由 Agent 自我遵守。
一、数据模型变更
typescript
const SCHEMA_VERSION = 6;   // 导入 version < 6 拒绝,不自动迁移

// TargetConfig 基本沿用 v4.3,仅调整 script 字段含义与新增 Agent 接入痕迹
interface TargetConfig {
  id: string;
  name: string;
  type: 'custom' | 'comfyui';
  capability: CapabilityTag;          // text | multimodal | image(沿用 v4.3 三标签)
  source: 'agent' | 'manual';         // ← 改:'ai-parsed' 改为 'agent'(由 Agent 自动接入)
  inputParams: ParamDef[];            // 由 Agent 整理并通过 save_target 传入

  script?: {
    lang: 'python';                   // 本期仅 python(node/shell 不支持)
    code: string;                     // Agent 跑通后的最终脚本
    verified: boolean;                // Agent 真跑通才 true
    lastTestInput?: string;           // ← 新增:最后一次测试塞入的参数(给用户确认用)
    lastTestOutput?: string;          // 最后一次测试的原始输出(给用户确认用)
    outputDir?: string;               // 脚本图片产物临时落地文件夹
  };

  comfyui?: { serverUrl: string; baseModel: string; loraName?: string; loraWeight?: number; };
  apiKeyRef?: string;
  status: 'unverified' | 'tested_ok' | 'tested_fail' | 'unsupported';
  rawDoc?: string;                    // 接入时粘贴的对接文档原文
  preset?: boolean;
}
其余数据模型(Project / Task / TaskInput / ResultItem / EvaluationRecord / GenDataRequest / ParamDef / CapabilityTag 三标签等)完全沿用 v4.3。

说明:

source 取值从 'ai-parsed' | 'manual' 改为 'agent' | 'manual'(Agent 自动接入 / 手动填改)。
script 新增 lastTestInput,与 lastTestOutput 一起在"用户确认建接口"时显著展示。
二、Agent 模型配置(干活的模型)
2.1 两类模型严格区分
← 可左右滑动查看更多内容 →
全屏
复制
类别	用途	选择自由度	tool use 要求
Agent 模型	后端干活的接入助手(写脚本/跑/装库/接接口)	固定 DeepSeek,用户接入其 Key	必须支持 function calling
测评目标模型	被对比测评的对象	完全自由接入/选择	不需要
2.2 Agent 模型接入
设置中单独一处「自动接入助手(Agent)模型」,固定为 DeepSeek(deepseek-chat / V4 Pro,以 DeepSeek 官方支持 function calling 的型号为准)。
用户须在 .env.local 配置 DEEPSEEK_API_KEY;未配置时,自动接入功能不可用并明确提示「请先配置 DeepSeek API Key 以启用自动接入」。
Agent 调用走 DeepSeek OpenAI 兼容端点(baseURL: https://api.deepseek.com),格式为 OpenAI function calling。
不提供"Agent 换其他模型"选项(避免用户选到不支持 tool use 的模型导致 Agent 失效)。
2.3 测评目标模型
内置预置目标(built-in)+ 用户通过 Agent 自动接入的目标(script)+ ComfyUI,沿用 v4.3 全部规则,自由接入/选择不变。
三、Agent 自动接入流程(板块② —— 取代 v4.3 第四章)
3.1 用户视角(极简)
板块②点「自动接入」→ 粘贴对接文档 → 点「开始」
   ↓
实时进度区显示 Agent 正在干嘛(写脚本→跑→缺库装requests→改→再跑→跑通…)
   ↓
Agent 跑通 → 显著展示三项【默认不折叠】:
   ① 本次测试塞入的输入参数
   ② 脚本真实返回的原始输出
   ③ 渲染结果(文本/图片懒加载)
   + Agent 的说明(这是什么接口/什么能力/要填哪些参数)
   ↓
用户确认结果真实有效 → 点「确认接入」→ 存为 TargetConfig(status=tested_ok)
   (脚本代码默认折叠,可展开查看/手动编辑)
   ↓
若 Agent 始终跑不通 → 展示失败原因 + Agent 给的解决建议(含终端命令)
   → 用户可:重试 / 手动改脚本后接入 / 强存 unverified
3.2 Agent 后端循环
后端 /api/agent-connect 启动 Agent 循环:
把 system prompt(见 3.4)+ 用户文档发给 DeepSeek。
DeepSeek 决定调工具 → 后端执行工具 → 结果喂回 → 循环。
直到 DeepSeek 不再调工具(完成)或达到循环上限。
循环上限(默认 30 步,config 可调),防失控烧 token。
全程通过 SSE / 流式把进度推给前端实时展示。
3.3 Agent 可用工具(tool use)
← 可左右滑动查看更多内容 →
全屏
复制
工具	作用	后端实现
run_script	本机执行 Python 脚本,返回 stdout/stderr/退出码	child_process 子进程 + 超时杀进程(Windows 杀进程树),沿用 v4.3 执行机制
install_package	pip 安装缺失的包	child_process 执行 pip install
save_target	跑通后保存为接口(暂存,待用户确认)	暂存为 pending,不直接写库,返回前端供用户确认后才真正存入
工具执行的安全/超时/key 注入/图片产物处理全部沿用 v4.3 第六章(stdin 传参、超时杀进程、key 经环境变量注入、图片转 base64 收进结果体系等)。
关键改动:这些约束不再由平台单独的 run-script API 死管,而是 Agent 通过 run_script 工具调用时,后端工具实现层统一保障。
3.4 Agent System Prompt(写进 config,核心)
system prompt 把原 v4.3 的"束缚"转化为"Agent 自我遵守的规矩":

你是 API 接入助手。任务:根据用户对接文档,编写 Python 脚本调用该 API,在本机实际运行测试,自主修复所有问题直到真跑通并返回有效结果,然后保存为可用接口。

工具:run_script(本机跑脚本,脚本从 stdin 读参数 JSON)、install_package(pip 装包)、save_target(验证通过后保存接口)。

流程(自主驱动,不要每步问用户):

读文档,判断接口能力:纯文本对话 / 看图输出文字 / 生成图片。
写 Python 脚本:参数从 stdin 读取(json.loads(sys.stdin.read())),不写死;API key 从环境变量读;异步 API(提交→轮询)在脚本内完成完整轮询;脚本最后必须 print 一行结果 JSON:{"text":"文本或空","images":["URL或本地路径",...]},这是判断成功的唯一依据。
调 run_script 用合理测试参数实际运行。
自主处理结果:缺库→install_package;有 bug→改脚本;鉴权/参数错→对照文档改;反复迭代直到输出非空结果 JSON。
判断能力标签:纯文字→text;能接图输出文字→multimodal;输出图片→image。
整理参数清单(名称/类型 string·number·boolean·image/是否必填/说明)。
调 save_target 传入:接口名、最终脚本、参数清单、能力标签。
用简短中文告诉用户:接口类型、要填哪些参数、测试返回了什么(供用户确认真实有效)。
铁律:必须真跑通才保存;结果 JSON 必须是 API 真实返回,不得编造/占位;环境缺 Python 或缺库时给清晰可照做的终端命令;全程自主完成。

四、后端 API 变更
删除(由 Agent 取代)
❌ /api/gen-script
❌ /api/run-script(作为独立接入测试入口)
❌ /api/fix-script
❌ SCRIPT/META/RESULT_JSON 的"标记解析"作为接入主流程(结果 JSON 格式约定仍在 Agent prompt 里,但平台不再围绕它做接入态机)
新增
POST /api/agent-connect    Agent 自动接入(SSE 流式)
  入参: { doc }            // 对接文档原文
  处理: 用 DEEPSEEK_API_KEY 启动 Agent 循环;
        循环中按需执行工具 run_script / install_package / save_target;
        循环上限(默认30,config可调);max_tokens 调大(≥8192)防截断
  流式推送(SSE): 每步进度 { type: 'thinking'|'tool'|'tool_result'|'done'|'error', ... }
  最终: save_target 产出 pending 接入草稿(含 code/params/capability/lastTestInput/lastTestOutput),
        返回前端待用户确认

POST /api/confirm-target   用户确认后真正存入接口
  入参: { pendingTarget }  // 用户确认/可能微调后的接入草稿
  处理: 存为 TargetConfig(script.verified=true, status=tested_ok)
  出参: { target }
保留(沿用 v4.3)
/api/comfyui/list、/api/comfyui/run、/api/gen-data、/api/gen-eval-prompt、/api/evaluate
body 上限 25MB,App Router Route Handler 写法。
内置预置目标(含裁判)走 built-in adapter 直调 HTTP,不经 Agent。
跑批时的脚本执行
跑批仍需执行已接入的脚本(用 TaskInput 真实值)。跑批不走 Agent(Agent 只管接入),而是后端有一个内部脚本执行能力(即原 run_script 工具的底层实现),跑批时直接调它:stdin 传参 → 执行 → 取结果 JSON → 归一化。
即:run_script 的"执行能力"保留为后端内部函数,接入时供 Agent 调用,跑批时供执行流程调用;只是不再暴露为独立的 /api/run-script 接入测试端点。
五、adapter 三类(沿用 v4.3,微调说明)
全屏
复制
adapter	服务对象	执行方式
built-in	内置预置目标(含裁判)	平台写死 HTTP 直调,不走脚本不走 Agent
script	用户经 Agent 接入的目标	跑批时后端执行已存的脚本(stdin 传参,内部脚本执行函数)
comfyui	ComfyUI	固定模板 + 异步三步
三类输出统一归一化 { outputText, outputImages[], latencyMs },上层禁 if-else。
Agent 仅用于"接入阶段";跑批阶段 script 目标直接执行已验证脚本,不再启动 Agent。
六、配置变更
.env.local 新增/明确:DEEPSEEK_API_KEY(Agent 模型必需);其余目标 key 沿用。
config/agent.ts(新增):
Agent 模型名(DeepSeek,固定)、baseURL、max_tokens(≥8192)
Agent 循环步数上限(默认 30)
Agent system prompt(见 3.4)
工具定义(run_script / install_package / save_target 的 schema)
config/runtime.ts(沿用 v4.3,调整):删除"脚本修复次数上限(3)"(已无 fix-script);保留脚本执行超时(60s,生图类可单独放宽,如 120s,config 可调)、脚本产物根目录(./outputs)、并发(3/上限10)、body 上限。
config/presetTargets.ts、config/comfyTemplate.ts 沿用 v4.3。
已删除 config/capabilities.ts(v4.2 已删)。
七、前端变更(板块②接入区)
「自动接入」改为主入口:粘贴文档 → 开始 → 实时进度区(消费 SSE,显示 Agent 每步:思考/写脚本/跑脚本/装库/报错/跑通)。
跑通后显著展示三项(输入参数 / 原始输出 / 渲染结果)+ Agent 说明 → 用户「确认接入」才存。
脚本代码默认折叠,可展开查看/手动编辑(手动兜底保留)。
失败时展示 Agent 给的原因 + 解决建议(含终端命令);提供 重试 / 手动改后接入 / 强存 unverified。
设置区分两处选模型:「自动接入助手模型(固定 DeepSeek)」与「测评目标模型(自由)」。
其余 5 板块(① 跑批 / ③ 结果与历史 / ④ AI评价 / ⑤ 评价结果与历史)完全沿用 v4.3。
八、明确不做(在 v4.3 基础上调整)
新增/调整:

不再用"平台死规则模拟接入"(无 gen-script/fix-script/标记解析作接入态机/修复3次上限);改用 Agent。
Agent 模型不可换成不支持 function calling 的模型(固定 DeepSeek)。
Agent 仅用于接入阶段,不介入跑批 / 评价 / 其他流程。
沿用 v4.3 / 更早:

模板接入、沙箱(本地自用本机主动执行,本期不做;对外多用户须另上)、shell 脚本(仅 python)、"脚本没崩算成功"、AI 把代码塞 JSON、测试通过自动建接口(仍须用户确认)、内置目标走脚本/Agent、contentKind 的 both 双语义、流式输出、任意 ComfyUI 工作流、AI 假数据自测、本地跑 SD/Diffusers、本地上传 LoRA、用户登录/云部署、批量本地图片、断点续传、Excel 图片打包 zip、纯前端直连(仅架构预留)。
九、验收标准
理想路径
配置 DEEPSEEK_API_KEY 后,自动接入可用;未配置则明确提示。
粘贴对接文档 → 点开始 → 进度区实时显示 Agent:写脚本→跑→(缺库)自动 install→改→再跑→真跑通输出非空结果 JSON。
跑通后展示输入参数 + 原始输出 + 渲染结果 + Agent 说明;用户「确认接入」→ 存为 tested_ok,能力标签(text/multimodal/image)正确。
异步 API(提交→轮询):Agent 写的脚本内部完成轮询并最终输出结果。
接入的脚本目标,跑批时用真实参数(stdin 传入)正常执行、结果归一展示;脚本图片转 base64 进结果体系,JSON 导出后另一环境可重现。
Agent 模型固定 DeepSeek;测评目标模型仍可自由接入/选择。
三标签、5 板块、评价(④存 evaluations / ⑤回看导出)、ComfyUI、跑批、IndexedDB、JSON 导出导入(version=6)全部沿用 v4.3 且正常。
异常路径(必测)
未配 DEEPSEEK_API_KEY → 自动接入禁用并提示。
Agent 循环达上限仍未跑通 → 展示失败原因 + 建议(含命令);用户可重试 / 手动改后接入 / 强存 unverified。
缺 Python / 缺库 → Agent 自主 install 或给出可照做的终端命令(装 python 指引)。
Agent 反复修复但脚本始终输出空结果 → 不保存,提示用户;绝不"没跑通就接入"。
Agent 想保存的接口,用户在确认页看出返回是示例/占位 → 用户拒绝接入。
run_script 超时 → 杀进程(Windows 杀进程树),进程不堆积;循环不失控(上限 30)。
DeepSeek 返回长脚本不再因截断报错(max_tokens≥8192);若仍异常,Agent 可在下一轮续写。
导入 version<6 → 拒绝并提示"版本过旧不兼容"。
沿用 v4.3 全部异常项(三标签置灰、生图费用弹框、评价洁癖、ComfyUI 连不上、部分成败 partial、取消清排队等)。
编程注意事项
Agent 在后端跑,不在浏览器;前端只发起 + 看进度(SSE)+ 确认接入。
DeepSeek function calling 用 OpenAI 兼容格式(tools / tool_calls);model 名以 DeepSeek 官方支持 function calling 者为准;max_tokens ≥ 8192;循环上限 30(config)。
工具实现层统一保障安全约束:run_script 用 child_process + 超时杀进程(Windows 杀进程树)、stdin 传参禁字符串拼接进 code、key 经环境变量注入不落明文、图片产物转 base64 进结果体系(沿用 v4.3 压缩规则)。
save_target 不直接写库:产出 pending 草稿 → 前端展示三项供用户确认 → /api/confirm-target 才真存(防假正确)。
跑批不走 Agent:script 目标跑批直接调内部脚本执行函数(原 run_script 底层),Agent 只管接入。
手动兜底保留:脚本/参数/能力标签全程可手动编辑;Agent 跑不通也可手动改后接入或强存 unverified。
其余(adapter 三类归一化禁 if-else、preset 不走脚本/Agent、核心数据原则值存 TaskInput、两维正交草稿、SCHEMA_VERSION=6 拒旧、IndexedDB debounce+配额提示、评价存 Project.evaluations、三标签 image 独占生图、Excel 列名 targetId、含图裁判须 multimodal 等)全部沿用 v4.3。
十、实施批次(在 v4.3 基础上调整第二批)
第一批:沿用 v4.3(骨架 / 三类 adapter 骨架 / IndexedDB / JSON version=6 / 两维输入 / 三标签筛选 / 并发·试运行浮窗·批量 / 结果区 / Excel 导出 / ③历史 / 5 板块导航 / built-in 预置目标跑通核心对比)。
第二批(Agent 接入,本次重点):
config/agent.ts(DeepSeek 配置 + system prompt + 工具定义)
后端内部脚本执行函数(child_process + 超时 + stdin 传参 + Windows 杀进程树 + 图片转结果体系)——同时服务 Agent 的 run_script 工具与跑批执行
/api/agent-connect(Agent 循环 + SSE 进度推送 + 循环上限 + max_tokens 调大)+ /api/confirm-target
前端:自动接入入口 + 实时进度区 + 三项确认页 + 手动编辑兜底 + Agent 模型(DeepSeek)配置项
ComfyUI 收窄(沿用 v4.3:list + 固定模板 + 三步)
第三批:沿用 v4.3(AI 造数据 + ④逐条评价存 evaluations + ⑤评价结果与历史)。