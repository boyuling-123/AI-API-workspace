# 项目能力总览与命名灵感

> 用途：把这个项目「实际做了什么、能干什么、怎么实现的」一次性讲清楚，并基于真实能力给出一组**不死板的新名字**备选，方便你重新命名、重塑网页气质。

---

## 一、一句话是什么

**让多个 AI / 算法接口同时回答同一道题，并排比一比谁更强；还能让 AI 自己接入新接口、让 AI 当裁判按多维度自动打分——一个本地就能跑的「AI 评测工作台」。**

- 形态：本地运行的网页工具（Next.js 应用，`npm run dev` 即用）。
- 用户：算法工程师、Prompt 工程师、AI 产品经理。
- 气质关键词：**横向对比、批量、自动化、量化评分、可玩**（角落还住着一只会漫游的像素小狗 🐕）。

---

## 二、整体能力地图（5 大板块 + 2 个彩蛋/辅助）

| 板块 | 名称 | 一句话 |
|------|------|--------|
| ① | 跑批 / 并排对比 | 选目标 + 写输入 → 多目标并发运行、并排出结果 |
| ② | 接口接入与管理 | 真 Agent 自动接入任意接口，或手动配置 |
| ③ | 结果与历史 | 看跑批结果、历史记录，一键导出 Excel |
| ④ | AI 多维度评价 | 让裁判 AI 按多个维度逐项打分、写点评、排序 |
| ⑤ | 评价结果与历史 | 回看所有历史评分报告，可导出 |
| 辅助 | AI 造数据 | 一句话描述 → AI 生成一批测试用例直接灌入批量 |
| 彩蛋 | 像素小狗助手 | 角落原创像素小狗，两侧留白漫游、随状态卖萌 |

---

## 三、核心能力详述

### 1. 多目标并排对比 + 批量评测（地基）

- **选目标**：勾选要 PK 的模型/算法。内置 DeepSeek V4 Pro、Kimi K2.6（多模态）、Qwen3.6 Plus（多模态·默认裁判）、Mock 生图算法，加上你自己接入的任意接口。
- **写输入**：单条直接打字；批量上传 Excel（每行一个用例）。
- **并发运行**：所有目标同时调用，结果**并排卡片**展示，文本 / 图片统一渲染。
- **试运行浮窗**：正式跑批前可「试运行」预览效果，不落历史。
- **全模态**：纯文本、看图出文字（多模态）、文生图/图生图（一次出多张图）都支持。
- **费用确认**：跑批前弹确认框，避免误触发大批量消耗。

### 2. AI 自动接入接口（最大亮点 ⭐）

把「接入一个新接口」从读文档 + 写脚本 + 调试的几小时，压缩成「**粘贴文档 → 等它跑通 → 确认**」的几分钟。

- 背后是一个**真正的 Agent 循环**（不是死规则模拟），给 AI 配了几个工具自主驱动：
  - `run_script`：本机真实执行 Python 脚本，拿真实 stdout/stderr
  - `install_package`：缺库自己 `pip install`
  - `save_target`：跑通后保存为可用目标
  - `ask_user`：缺 Key / 缺参数 / 文档不清时，内嵌对话框**主动追问**用户
- 行为像 Claude Code：读文档 → 写脚本 → 自测 → 装库 → 看报错 → 反复改，直到**真跑通**才保存。
- 进度实时流式推送（SSE），循环有步数上限防失控。
- **辅助：AI 解读文档**——粘贴看不懂的接口文档，AI 帮你读出地址、参数、鉴权方式（只告诉你、不乱填，最后你确认）。
- **手动兜底**：跑不通时可手动改脚本接入，或强存为未验证状态。

### 3. 结果与历史

- 结果按「输入 × 目标」表格展示，文本可展开、图片可点开放大。
- 所有跑批记录持久化（IndexedDB，刷新不丢）。
- 一键导出 Excel（入参、出参、模型、耗时全在内）。

### 4. AI 多维度评价（核心亮点 ⭐）

- 选一个「裁判 AI」。
- 一句话说清「想评什么」→ AI **自动生成一批评分维度**（待办清单式勾选，可勾可不勾、可手动加、可「再来一批」追加，上限 15 条；内置预设维度只作模型参考、不直接透出）。
- 按勾选的维度**自动生成评分 Prompt**。
- 裁判 AI 对每个目标**按维度逐项打分 + 写理由 + 给总体点评**，结果表格每个维度一列、可按维度排序。
- 评价结果独立留存，同一批次可多次评价、互不覆盖。

### 5. 评价结果与历史

- 历史评分报告倒序列出（时间 / 来源批次 / 裁判模型 / 条数）。
- 展开详情：入参 | 出参 | 模型 | 各维度分 + 理由 | 总体点评。
- 每条评价可导出 Excel（每维度独立「分/理由」列）。

### 6. AI 造数据（辅助）

一句话描述想要的测试数据，AI 自动生成一批结构化用例，直接灌入批量评测。

### 7. 像素小狗助手（彩蛋 · 让网页不死板的关键 🐕）

- 角落一只**原创像素艺术风格小狗**（纯代码绘制 SVG，无版权风险，可安全公开）。
- 窗口够宽时在**两侧留白竖带自主漫游**（慢速平滑、随机选点、停顿发呆、朝向翻转），窗口窄了自动缩回右下角待机。
- **随平台状态卖萌**：跑批/接入/评价进行中 → 专心 busy；成功 → 开心；失败 → 难过；点它 → 冒气泡说一句话。
- 可拖动、可收起/唤出，偏好持久化。纯装饰、只读状态、不碰任何业务数据。

---

## 四、技术架构（怎么实现的）

### 技术栈

| 层 | 技术 |
|----|------|
| 框架 | Next.js 14（App Router） |
| 前端 | React 18 + TypeScript + TailwindCSS |
| 本地存储 | Dexie（IndexedDB 封装） |
| Excel | SheetJS（xlsx） |
| AI 调用 | DashScope（百炼）Anthropic 兼容网关 |

### 分层结构

```
src/
  app/         页面 + API 路由（agent-connect / evaluate / gen-data / gen-dimensions ...）
  components/  UI 组件（5 大板块 + 输入/结果/评价/宠物等）
  hooks/       业务逻辑 hooks（useEvaluation / useTaskRunner / useProject ...）
  services/    调用层（agent / evaluateService / genDimensionsService ...）
  adapters/    目标 adapter（输出统一归一化）
  config/      集中配置（agent / models / runtime / presetTargets / petConfig ...）
  lib/         工具函数（taskRunner / petBus ...）
  types/       数据模型 interface
```

### 关键设计

- **adapter 归一化**：内置模型直调 / 脚本目标 / ComfyUI 生图工作流，输出统一归一为 `{ outputText, outputImages[], latencyMs }`，上层无 if-else。
- **Key 安全**：前端只存环境变量「引用名」，真实 Key 在服务端 `.env.local` 注入，绝不经过前端。
- **Agent 后端循环**：`/api/agent-connect` 启动循环、SSE 流式推进度、步数上限防失控、支持 `ask_user` 暂停/恢复。
- **脚本执行隔离**：子进程执行、超时杀进程、stdin 传参、图片产物转 base64 进结果体系。
- **宠物与业务解耦**：全局事件总线（`petBus`）只「广播」状态，宠物订阅切表情，绝不读写业务数据。

---

## 五、产品亮点小结

| 亮点 | 价值 |
|------|------|
| 🤖 真 Agent 自动接入 | 粘贴文档就能接入任意接口，自写自测自修，门槛趋近于零 |
| ⚖️ AI 当裁判·多维度 | 维度 AI 生成、逐维打分点评，评测从主观变量化 |
| 📊 多目标并排 + 批量 | 多模型/算法横向对比，Excel 批量一次跑完 |
| 🖼️ 全模态 | 文本 / 多模态 / 文生图图生图统一支持 |
| 🔌 可扩展架构 | 三类 adapter 归一化，新增接口类型不动上层 |
| 🔐 Key 不落前端 | 真实 Key 仅服务端注入 |
| 🐕 像素小狗陪伴 | 角落漫游卖萌，让工具有温度、不死板 |

---

## 六、命名灵感（基于真实能力起名）

> 核心意象可选：**擂台 / 裁判 / 对比 / 竞技场 / 自动接入 / 陪伴感**。下面按风格分组，每个附一句调性说明。

### A. 竞技场 / PK 风（突出「并排对比、谁更强」）

- **ModelArena（模型竞技场）** — 让模型同台竞技，一眼见高下。
- **PromptRing（提示词擂台）** — 把 Prompt 丢进擂台，模型上场 PK。
- **VS.AI / 模型擂台** — 简洁直接，主打对比与对决。
- **EvalRing（评测擂台）** — 评测 + 擂台，定位清晰。

### B. 裁判 / 评审风（突出「AI 当裁判、多维度打分」）

- **AIRefree / AI 裁判席** — 强调自动评分这个核心差异点。
- **JudgeBench（裁判台）** — 裁判 + 基准测评，专业感。
- **VerdictAI（评判 AI）** — 「下判决」的意象，干脆利落。

### C. 工作台 / 轻松风（突出「本地工具、好用、有温度」，配像素小狗气质）

- **PupEval（小狗评测）** — 直接把像素小狗变成品牌符号，活泼好记。 ⭐
- **EvalPup / 评测旺财** — 萌系工具人设，去死板感最强。
- **ModelLab（模型实验室）** — 沉稳工程范。
- **TestPad（评测台）** — 轻量、像便签一样随手可用。

### D. 玩味 / 个性风（最不死板）

- **斗模 / 斗模阁** — 「斗茶」式联想，模型互斗，有国风趣味。
- **谁更强（WhoBetter）** — 把核心问题直接当名字，记忆点极强。
- **小狗当裁判 / 旺财评测局** — 把彩蛋拉成主品牌，独一无二。 ⭐

> 个人推荐方向：若想**专业又有记忆点**选 `ModelArena / JudgeBench`；若想**彻底不死板、有人格化温度**，把像素小狗做成吉祥物，选 `PupEval / 旺财评测` 这一挂——名字、UI 配色、文案、那只漫游小狗能形成统一的轻松调性。

---

## 七、配套换名要改哪些地方（备查）

重新命名后，若要让整个网页气质统一，通常会动到：

- `package.json` 的 `name`
- `src/app/layout.tsx` 的页面 `<title>` / metadata
- 顶部工具栏标题（`src/components/TopToolbar.tsx`）
- README、PRD 文档标题
- （可选）配色主题、favicon、宠物形象与品牌呼应

需要的话我可以按你定下的新名字，一次性把这些位置全部改好。
