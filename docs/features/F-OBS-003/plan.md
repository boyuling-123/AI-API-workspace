# F-OBS-003 执行计划

按 Developer Helper 的 Planner → Developer → Tester → 发布循环执行，不冒充独立人类审查。

1. 锁定公开依赖、审计回调与遥测，实际运行无模型 StateGraph 探针；已完成，增加 20 个包，audit 仍为原有 7 项（6 high/1 low）。
2. 单独编译固定运行入口；隔离环境和网络，实际回调转 OTel；不引入 LangSmith 服务、Graph API 服务或持久化 checkpointer。
3. 添加本机 API 与中文独立运行按钮，复用步骤检查器；旧数据契约可读，新增明确的框架来源元数据。
4. 用真实子进程/框架做单测，Mock 仅用于节点；安全守卫、生命周期、前端用户路径、可访问性、截图与完整质量门禁。
5. 更新 OSS 复用、能力矩阵、证据、台账；正常提交/推送/PR，检查最终 CI 与合并条件后正常合并。

回滚：正常 revert 本主题提交，恢复旧 Mock 按钮与契约；没有数据库迁移、用户数据写入或系统设置变更。
