# P0-A / N13：共享观测能力声明校准

状态：已验证；299unit/2stress/quality、4专项和全量67 E2E无重试通过，截图/Trace及[PR #55](https://github.com/boyuling-123/AI-API-workspace/pull/55)最终CI完成，已正常合并。[证据](../../evidence/pr-capability-claims/README.md)。基线：PR54合并4d7d7f9，分支`codex/fix-observability-capability-claims`。这是F-ACT-001文案缺陷修复，不新增产品能力计数。

## 缺陷与范围

N12发现 `ACTION_LIMITS` 仍写“不代表真实框架适配”，但真实LangGraph固定Mock节点已在PR51验证。改为准确说明固定真实调度、零模型、任意用户Agent未兼容，以及观测文件来源未认证且不重放。保留既有UI/API/MCP共享同一响应，工具数量、名称、Schema、权限和业务执行不变。

## 完成条件

| 条件 | 验证 |
|---|---|
| 不再否认已验证的固定LangGraph调度，也不扩张为任意Agent兼容 | 真实Action和API语义断言，页面文字断言 |
| 回读文件不能冒充现场执行认证或Agent重放 | 共享声明/结构化返回一致性及浏览器检查 |
| 仍仅两项只读工具，不自动调用模型/归档/实验 | 原工具名、空参数、读权限、调用计数、请求Guard |
| 两种实际MCP协议路径返回相同声明 | 正式SDK客户端到已编译stdio服务端测试 |
| 页面正常/失败/重试/离开重入/移动端/可访问性可用 | 现有3条路径回归+新声明路径，截图和成功Trace |
| 文档、状态、SecretScan、完整质量与最终CI可追溯 | 台账、纪实、证据、PR和正常合并 |

## 实施计划

1. 修改一份领域声明，不复制到组件或协议层，不引入依赖。
2. 加真实Action/API/正式MCP断言；浏览器用真实本机API验证声明、结构化JSON、刷新不自动运行，继续使用合成归档与外部请求阻断。
3. 仅停本工作树3002预览，执行quality和全量E2E，截图/成功Trace视觉复核后恢复预览。
4. 回写审计报告P0-A解决范围、台账、纪实和证据。原75项、九个夜间能力数量不变。自动PR、最终headCI后普通合并，不伪造独立Approve。

## 回滚

普通revert本修复即可恢复声明和断言；没有数据或Schema迁移。若回滚，需继续公开记录旧声明过时，不能将问题标为已解决。
