# N14 早间演示与实验交付

状态：已验证，[PR #56](https://github.com/boyuling-123/AI-API-workspace/pull/56)最终head b410808的CI34067940111全成功，07:56:54已正常合并484d40a。原分支`codex/docs-morning-handoff`，基线PR55合并`3c625c7`。不新增业务能力计数，不修改平台运行代码或存储。实际采集、299unit/2stress/67E2E、截图与ZIP校验见[验收证据](../evidence/pr-morning-handoff/README.md)。

## 产物与计划

1. 使用现有Playwright依赖编写可复用的合成证据采集脚本，只访问回环观测页和固定LangGraph实验接口；其他API/外部请求阻断，不读取密钥或归档正文。
2. 从真实网页分别运行一次手工OTel三组流程和真实LangGraph两组固定Mock节点；实际下载原JSON，取消一次预览后再确认回读，验证再导出仅新增未认证来源标记。
3. 记录网页实际表格、采集时间、版本、代码SHA、文件哈希和请求数，不复制实验算法，也不编造性能结果。采集工具不自动执行评分、读取归档或管理浏览器项目。
4. 编写中文早间报告，公开产物仅包含本次固定合成JSON、Manifest、截图与说明。ZIP按明确白名单打包，不递归复制项目/私密目录。
5. 采集器属于开发验收脚本，执行真实网页路径、大小/来源/往返/零隐式调用/外部阻断/390px/axe断言；完整quality与既有E2E回归，再创建PR并等最终head CI。

## 验收与界限

- 两份原结果分别为3 Trace/14 Span与2 Trace/7 Span，模型调用0，Token/成本未测量。场景是合成工程实验，不是模型准确率、标准Benchmark或框架性能排名。
- 回读前不替换结果，取消不执行，确认后结果标记来源未认证；重新导出保留该标记，不把文件内容认作现场执行签名。
- Manifest与报告的数量、根状态、中间失败和展示耗时来自实际产物/网页；若数字不符则停止交付，不修饰成功率。
- 不同环境时钟和人为8ms等待会影响耗时，单次采集不具有统计性能意义；不得把多个策略当独立真实业务数据规模。
- 历史归档仍为既有117065索引/55444原始ID，不是本夜新跑批；不进入合成ZIP或公开截图。
- 包含开发脚本时不按“纯文档”跳过门禁；N12旧统计与能力矩阵状态保持不变。

## 回滚与截止

普通revert只移除说明、合成产物和开发采集脚本，不修改用户存储。08:40停止领任务，09:00停止开发/测试/提交/推送并暂停原automation；预览保留供用户查看，防睡眠仅本夜到期释放。
