# PR 04B 验收证据

## 视觉证据

- `hard-rules-bad-case.png` 展示每行一条的硬规则输入、`2/20` 规则计数、两条代表性样本、一条 Bad Case 标记与必填原因。
- 截图由 Playwright 在 `1280 × 1200` 视口、页面回到顶部后生成，并已人工检查，无粘滞页头重叠、遮挡或截断。

## 自动化证据

- `tests/e2e/dimension-rules.spec.ts` 全程 Mock：原因为空时维度请求为零，补齐后恰好一次 `/api/gen-dimensions`，`/api/evaluate` 始终为零。
- 精确请求断言覆盖两条硬规则、Bad Case 原因和失败类型，并确认原始图片 base64、完整失败文本与敏感值未发送。
- 同一路径执行 Axe WCAG 2.0/2.1 A/AA 扫描，严重与致命问题为零。
- 本地 `npm run quality` 通过 224 文件密钥扫描、67 项单测、2 项压力测试和 19 路由构建；`npm run test:e2e` 通过全部 16 项用户路径。
- 提交 `a054594` 在独立 `/tmp` 工作树全新安装 434 个包后重复通过两道门禁，结束时 Git 零改动。

## 安全边界

- 测试未读取真实密钥、未调用真实或付费模型，也未启动 AI 评价。
- 客户端和服务端共享脱敏实现；页面提示用户不要粘贴密钥或完整日志，服务端仍把脱敏作为强制兜底。
