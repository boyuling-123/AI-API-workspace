# PR 06A 验收证据：黄金集领域模型与严格导入

## 自动化路径

- `tests/unit/goldenDataset.test.ts` 直接覆盖真实领域与导入源文件，共 8 项测试。
- 导入测试覆盖双语字段映射、真实 Excel 工作簿、严格 JSON 容器、非法标签、重复 Case ID 和越界分数。
- 版本测试覆盖 v1 深拷贝、v2 追加、旧版不变、变更说明门禁、缺失家族、篡改检测和敏感值脱敏。
- 功能提交 `3e0582a` 在独立 detached 工作树全新安装 434 个包后，通过 quality 与既有 22 项 Playwright，结束时 Git 零改动。

## 安全边界

- 本 PR 只建立数据结构、严格解析结果与不可变版本，不新增页面动作，也不会发起任何 API 或 Judge 请求。
- 结构化解析问题由后续 UI 显式展示；本层不会自动跳过、补空或猜测缺失的人工标签。
- 锁文件仍有既有的 6 个 high 级依赖审计项；未执行可能引入破坏性升级的 `npm audit fix --force`。
