# 测评平台 PRD v4.9：后续优化路线图

> 文档日期：2026-07-14
> 适用项目：本地大模型评测平台
> 核心目标：把平台从“能跑小规模评测的页面工具”，升级为“可被外部 skill / agent 召唤、可承载大批量数据、可安全执行 AI 评价的评测工作台”。

## 1. 背景

当前平台已经具备基础评测能力，包括输入数据维护、AI 造数据、批量运行、跑批历史、AI 评价、导入 API、skill 召唤导入等方向。但随着使用场景从少量样本扩展到微调模型评测、标准答案评测、3000 条级别数据生成与跑批，现有交互和状态管理还需要继续收敛。

后续优化的重点不是单独加按钮，而是把每个关键流程整理成稳定的状态机：

- AI 造数据要能显示清楚进度，支持暂停、保留、清空、续跑、下载。
- 输入数据表要能承载大批量数据，不因为 3000 行 input 卡死。
- AI 评价要拆成可外部调用的 API 能力，但不能默认消耗裁判模型额度。
- skill 要作为“测评平台召唤器”，负责识别数据、确认字段、生成导入包、调用平台、返回真实链接。
- 跑批历史和 AI 评价历史要能分页、筛选、导出，不能一打开历史就全量渲染。

## 2. 产品定位

平台定位为“评测工作台”，服务三类入口：

- 页面入口：用户直接在 HTML 页面里导入数据、造数据、跑批、评价。
- API 入口：外部脚本、agent、skill 可以创建导入草稿、评价草稿、跑批计划。
- skill 入口：用户用自然语言和原始文件召唤平台，由 skill 完成字段确认和导入。

平台不应变成“传一段任意文本就自动硬解析、自动跑批、自动评价”的黑盒。所有高成本或不可逆动作都必须明确确认。

## 3. 非目标

- 不默认启动 AI 评价，因为评价会消耗裁判模型调用。
- 不在未确认标准答案字段时自动做 reference 评价。
- 不把完整大数据塞进 URL，URL 只允许放短 ID 和页面状态。
- 不把所有外部 API 能力堆到首页，应在对应功能模块旁边体现。
- 不为了展示方便一次性渲染 3000 行输入或上万行结果。
- 不伪造 open_url。平台异常时必须返回真实错误和诊断。

## 4. 当前能力基线

### 4.1 已有能力

- 支持输入数据的手动新增、Excel / JSONL 导入。
- 支持 AI 造数据，并已开始支持分批生成。
- 支持下载当前数据。
- 支持暂停大批量生成，并选择保留或丢弃已生成数据。
- 支持当前输入表格分页展示。
- 已有平台导入 API：`POST /api/import-evaluation-workspace`。
- 已有 `eval-platform-importer` skill，用于识别数据、生成导入包、调用平台 API。

### 4.2 当前体验问题

- AI 造数据过程中，进度信息容易被临时文案覆盖，用户看不到“总共多少条 / 当前已生成多少条 / 当前第几批”。
- 选择保留已生成数据后，用户需要明确的一键清空入口，且清空要同步取消续跑草稿。
- 继续生成剩余数据时，已保留数据必须继续留在下方表格，不能只记录数量。
- 输入表格需要更明确区分“完整数据集”和“当前页可见数据”。
- 跑批历史和 AI 评价历史仍存在大数据量渲染风险。
- AI 评价外部 API 能力需要产品化，不只是内部函数。
- skill 调用平台失败时，需要把诊断和恢复建议做得更自动。

## 5. P0：AI 造数据流程重构

### 5.1 目标

AI 造数据要成为一个稳定、可中断、可续跑、可下载、可清空的大批量生成流程。

### 5.2 核心交互

AI 造数据区域固定展示一个“生成状态卡片”，不依赖零散提示文案。

状态卡片展示：

- 目标条数：例如 `3000`
- 已生成条数：例如 `210`
- 剩余条数：例如 `2790`
- 当前批次：例如 `第 7 / 100 批`
- 每批大小：例如 `30 条 / 批`
- 当前状态：空闲、生成中、暂停中、已暂停、已完成、失败
- 最近一次错误：如果有，显示失败批次和错误原因

### 5.3 暂停与保留

当生成数量大于 100 条时，用户点击“暂停生成”后必须询问：

- 保留已生成数据：停止后保留表格中已有数据，下次只生成剩余数据。
- 丢弃本次生成：停止后恢复到生成前的数据。

保留逻辑必须保存真实数据 payload，而不是只保存已生成条数。

### 5.4 续跑逻辑

续跑时必须满足：

- 原本保留的 150 条仍然留在表格下方。
- 点击“继续生成剩余 X 条”后，只请求剩余数据。
- 新生成数据 append 到保留数据之后。
- 表格页码默认跳到最新页，但用户可以回到第一页。
- 如果用户修改了需求、字段、内容模式或目标条数，续跑草稿失效，改为重新生成。

### 5.5 清空逻辑

AI 造数据区域需要提供“清空当前数据”入口。

清空规则：

- 清空前必须确认。
- 清空当前表格数据。
- 清空续跑草稿。
- 清空生成进度状态。
- 如果正在生成中，清空按钮禁用或提示先暂停。
- 清空后不能再通过“继续生成”把旧数据种回来。

### 5.6 下载逻辑

下载当前数据按钮继续保留。

下载规则：

- 下载完整当前数据，不只下载当前分页。
- 数据来源不限于 AI 生成，也包括手动输入、Excel / JSONL 导入、skill 导入。
- 文件名应体现项目名和数据类型。
- Excel 字段包含 `prompt`、`image_url` 和所有 `extraFields`。

### 5.7 验收标准

- 生成 3000 条时页面不卡死。
- 每完成一批，表格和进度同时更新。
- 暂停后选择保留，表格仍保留已生成数据。
- 继续生成时不会覆盖已保留数据，只生成剩余数据。
- 清空后表格为空，续跑按钮消失。
- 下载 Excel 包含全部当前数据。

## 6. P0：输入数据表重整

### 6.1 目标

让输入数据表成为“完整数据集的可视化窗口”，而不是全量 DOM 渲染区域。

### 6.2 功能要求

- 超过 50 条后分页展示。
- 明确提示当前展示范围，例如 `当前展示第 151 - 200 条，共 3000 条`。
- 支持首页、上一页、下一页、跳到最新。
- 行号使用全局序号。
- 删除单行后页码保持合理位置。
- 下载、跑批、评价都基于完整数据集，而不是当前页。

### 6.3 后续增强

- 增加“清空当前数据”全局入口，可以放在输入区工具栏。
- 增加“批量删除当前页”。
- 增加“只看缺字段行”。
- 增加“按 prompt 搜索”。
- 增加“导出当前筛选结果”和“导出全部数据”的区分。

### 6.4 验收标准

- 3000 条输入只渲染当前页。
- 翻页和编辑不卡顿。
- 删除当前页最后一条时不会跳到空页。
- 所有下游动作读取完整 inputs。

## 7. P1：AI 评价能力外置化

### 7.1 目标

把 AI 评价从页面内按钮拆成可复用能力，让外部 skill / agent / 脚本可以创建评价草稿、预览字段、启动评价、查询进度、停止评价、导出结果。

### 7.2 API 能力拆分

建议新增：

- `POST /api/evaluation-drafts`：创建评价草稿，不启动评价。
- `GET /api/evaluation-drafts/:draft_id`：查看草稿和字段映射。
- `POST /api/evaluation-drafts/:draft_id/confirm`：确认输入字段、标准答案字段、评价模式。
- `POST /api/evaluations/start`：显式启动评价。
- `GET /api/evaluations/:evaluation_id/progress`：查询评价进度。
- `POST /api/evaluations/:evaluation_id/stop`：停止评价。
- `GET /api/evaluations/:evaluation_id/results`：分页获取评价结果。
- `GET /api/evaluations/:evaluation_id/export`：导出评价结果。

### 7.3 安全规则

- API 默认只创建草稿，不启动评价。
- 启动评价必须显式调用 start API。
- 超过 100 条评价必须确认。
- 超过 500 条评价显示强提醒。
- 超过 1000 条评价建议抽样。
- reference 模式必须确认标准答案字段。
- 缺失标准答案的行默认跳过，除非用户选择补空。

### 7.4 页面体现

AI 评价区域应显示外部能力入口，而不是放到首页：

- “通过 API 创建评价草稿”
- “外部启动评价需要二次确认”
- “查看评价 API 文档”
- “复制当前评价配置 JSON”

### 7.5 验收标准

- 外部 API 能创建评价草稿。
- 页面能打开外部创建的评价草稿。
- 用户确认后才能启动评价。
- 评价过程中可查询进度、停止、导出。
- 未确认标准答案字段时不能启动 reference 评价。

## 8. P1：skill 召唤平台增强

### 8.1 目标

让 `eval-platform-importer` skill 成为稳定的“测评平台召唤器”。

### 8.2 标准流程

skill 必须执行以下流程：

1. 识别原始数据格式。
2. 展示字段映射预览。
3. 询问用户确认输入字段、标准答案字段、评价模式。
4. 检查缺失值。
5. 让用户选择缺失策略：跳过、补空、停止。
6. 生成导入包。
7. 展示导入包摘要，等待最终确认。
8. 调用平台 API。
9. 返回真实 open_url。

### 8.3 服务诊断

skill 需要内置诊断：

- 检查 `127.0.0.1:3000`
- 检查 `localhost:3000`
- 检查首页状态码
- 检查导入 API 是否存在
- 区分连接失败、500、404、超时、代理问题
- 禁用本地请求代理干扰
- 不伪造链接

### 8.4 导入包规范

导入包必须包含：

- `project_name`
- `import_type`
- `dataset`
- `evaluation`
- `missing_policy`
- `target_hint`
- `created_by`
- `warnings`

### 8.5 验收标准

- Excel / CSV / JSONL / JSON 都能进入字段预览。
- 没确认标准答案字段时不会自动导入 reference 评测。
- 缺失字段有明确策略。
- 平台异常时 skill 给出中文诊断和重试建议。
- 成功时返回平台真实 open_url。

## 9. P1：跑批历史优化

### 9.1 目标

跑批历史要支持大批量结果查看，不能打开一次历史就渲染几千到上万行。

### 9.2 功能要求

- 历史列表只显示摘要。
- 点击详情后分页展示结果。
- 默认每页 50 条。
- 支持按目标、状态、关键词筛选。
- 支持只看失败项。
- 支持失败项重跑。
- 支持导出完整跑批结果。

### 9.3 历史摘要字段

每条历史至少展示：

- 批次名称
- 输入条数
- 测试目标数量
- 总运行条数
- 成功数量
- 失败数量
- 开始时间
- 耗时
- 状态：完成、部分完成、失败、已停止

### 9.4 验收标准

- 3000 条输入、3 个目标时，历史详情不会卡死。
- 失败项可以筛选。
- 导出包含完整结果，不受分页影响。
- 部分完成的跑批不误显示为完成。

## 10. P1：AI 评价运行与历史优化

### 10.1 目标

AI 评价要同时解决成本保护、分批执行、结果分页和历史回看。

### 10.2 运行保护

启动评价前必须展示：

- 本次评价条数。
- 本次评价目标数量。
- 本次评价维度。
- 裁判模型。
- 是否使用标准答案。
- 预计调用规模提示。

### 10.3 分批评价

评价运行必须分批：

- 默认每批 10 到 30 条。
- 每批完成后保存结果。
- 支持停止评价。
- 停止后已完成结果保留。
- 失败批次可重试。
- 评价状态必须展示已评价条数 / 总条数。

### 10.4 历史详情

评价历史详情应改为：

- 顶部摘要。
- 分页明细。
- 低分项筛选。
- 失败项筛选。
- 按维度排序。
- 导出完整 Excel。

### 10.5 验收标准

- 3000 条评价不会默认全量启动。
- 用户确认成本后才能启动。
- 评价可停止。
- 停止后结果可查看、导出。
- 历史详情分页打开不卡。

## 11. P2：平台 API 能力地图

### 11.1 目标

把平台能力拆成可被外部引用的小 API，而不是只有页面按钮。

### 11.2 能力清单

输入与导入：

- 导入数据集。
- 创建输入草稿。
- 获取字段映射预览。
- 导出当前输入数据。

测试目标：

- 查询测试模型 / 算法列表。
- 通过 `target_id` 引用测试目标。
- 创建临时测试目标。

跑批：

- 创建跑批计划。
- 启动跑批。
- 查询跑批进度。
- 停止跑批。
- 获取跑批结果。
- 导出跑批结果。

AI 评价：

- 创建评价草稿。
- 确认评价字段。
- 启动评价。
- 查询评价进度。
- 停止评价。
- 获取评价结果。
- 导出评价结果。

### 11.3 页面展示原则

外部 API 能力必须在对应模块旁展示：

- 输入区展示导入 API / skill 召唤入口。
- 测试模型区展示 target_id 引用方式。
- 跑批区展示跑批计划 API。
- AI 评价区展示评价草稿 API。
- 历史区展示结果查询和导出 API。

## 12. P2：持久化与草稿管理

### 12.1 目标

避免刷新页面、浏览器崩溃、服务重启后丢失重要草稿。

### 12.2 功能要求

- 输入数据草稿持久化。
- AI 造数据续跑草稿持久化。
- 外部导入草稿持久化。
- 评价草稿持久化。
- 跑批计划持久化。
- 提供草稿列表和一键清理。

### 12.3 验收标准

- 刷新页面后可恢复最近一次输入草稿。
- 外部 skill 导入后的草稿可再次打开。
- 用户能手动删除不需要的草稿。

## 13. P2：可观测性与错误诊断

### 13.1 目标

让用户知道平台为什么失败，以及下一步该怎么做。

### 13.2 功能要求

- API 错误返回结构化 `code`、`message`、`suggestion`。
- 前端展示用户能理解的中文错误。
- AI JSON 解析失败时展示原始片段和建议。
- 平台服务 500 时给出重启建议。
- skill 调用失败时输出诊断报告。
- 大批量任务记录失败批次和重试次数。

### 13.3 验收标准

- 用户不会只看到“失败”两个字。
- 错误信息能指向可执行动作。
- 技术细节可折叠展示，不污染主流程。

## 14. 里程碑建议

### M1：AI 造数据体验闭环

- 固定生成状态卡片。
- 恢复总数 / 已生成 / 批次进度。
- 完善暂停、保留、续跑、清空、下载。
- 输入表格分页细节修正。

### M2：跑批历史大数据化

- 历史摘要列表。
- 详情分页。
- 失败项筛选。
- 完整导出。

### M3：AI 评价安全运行

- 评价范围选择。
- 成本确认。
- 分批评价。
- 停止评价。
- 部分完成状态。

### M4：AI 评价 API 化

- 评价草稿 API。
- 评价确认 API。
- 评价启动 API。
- 进度 / 停止 / 结果 / 导出 API。
- 页面对应模块展示 API 入口。

### M5：skill 召唤平台完善

- 中文 skill 文档完善。
- API 文档完善。
- 平台服务诊断增强。
- 示例导入包完善。
- 与评价草稿 API 打通。

### M6：草稿持久化与诊断体系

- 输入草稿。
- 续跑草稿。
- 外部导入草稿。
- 评价草稿。
- 结构化错误和诊断报告。

## 15. 优先级总表

| 优先级 | 模块 | 动作 | 价值 |
| --- | --- | --- | --- |
| P0 | AI 造数据 | 固定进度状态卡片 | 解决用户不知道生成进度的问题 |
| P0 | AI 造数据 | 保留 / 丢弃 / 续跑状态机 | 防止中断后数据丢失或错乱 |
| P0 | AI 造数据 | 清空当前数据并取消续跑草稿 | 防止旧数据幽灵恢复 |
| P0 | 输入表格 | 分页与完整数据集语义 | 防止 3000 行卡死 |
| P1 | 跑批历史 | 摘要 + 分页详情 | 支持大批量跑批回看 |
| P1 | AI 评价 | 成本确认 + 分批评价 | 防止误触发大额裁判调用 |
| P1 | AI 评价 | 评价历史分页 | 支持大批量评价回看 |
| P1 | 外部 API | 评价草稿 API | 让 AI 评价可被 skill / agent 调用 |
| P1 | skill | 字段确认与服务诊断增强 | 提升召唤平台成功率 |
| P2 | 持久化 | 草稿管理 | 减少刷新和异常导致的数据损失 |
| P2 | 诊断 | 结构化错误 | 降低排障成本 |

## 16. 最终验收场景

### 场景 A：3000 条 AI 造数据

用户输入需求，选择生成 3000 条。平台按 30 条一批生成，状态卡片显示当前批次、已生成数量、剩余数量。生成到 210 条时用户暂停，选择保留。表格保留 210 条，用户可以下载，也可以继续生成剩余 2790 条。用户点击清空后，表格为空，续跑状态消失。

### 场景 B：外部 skill 召唤平台

用户在 Claude Code / Codex 中提供一份 Excel 和需求。skill 识别字段，展示输入字段、标准答案字段、评价模式和缺失策略预览。用户确认后，skill 生成导入包，调用平台 API，返回真实 open_url。打开链接后，平台已加载数据和评价草稿，但不会自动启动评价。

### 场景 C：标准答案 AI 评价

用户导入带 `gold_answer` 的数据集。平台要求用户确认标准答案字段和评价维度。用户选择评价 100 条样本并确认成本提示。平台分批评价，过程中可停止。停止后已完成结果保存为部分完成记录，可分页查看和导出。

### 场景 D：跑批历史回看

用户对 3000 条数据和 3 个目标执行跑批。历史列表只展示摘要。点击详情后分页查看 9000 条结果，支持筛选失败项，并可导出完整 Excel。

## 17. 风险与注意事项

- 大批量生成不能依赖模型一次性返回完整 JSON。
- 续跑必须保存真实数据，而不是只保存计数。
- 清空数据必须同步清空续跑草稿。
- 外部 API 不能绕过用户确认直接启动高成本任务。
- 页面分页不能影响下载、跑批、评价读取完整数据。
- 历史详情必须避免全量渲染。
- 平台 deep link 只能承载短 ID，不承载完整数据。

## 18. 下一步建议

优先做 M1。因为 AI 造数据是当前最容易被用户直接感知、也最容易出状态错乱的模块。M1 做完后，平台在 3000 条数据生成场景下会明显稳定，后续再继续推进跑批历史、AI 评价外置化和 skill 打通。
