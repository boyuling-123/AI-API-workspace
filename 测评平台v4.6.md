PRD v4.6 修订 · 电子宠物(小猪助手)
增量修订,只新增"电子宠物"这一个轻量彩蛋功能,其余全部沿用 v4.5。纯前端,不涉及核心数据/接入/评价逻辑,SCHEMA_VERSION 不变(仍为 7);宠物自身的少量偏好状态单独存储。

〇、目的
在网站角落放一只原创小猪助手作为电子宠物,提升趣味与陪伴感;会浮动、眨眼、点击说话,并能根据平台运行状态切换表情。纯装饰彩蛋,可隐藏、不干扰任何核心操作。

一、形象与版权(重要)
形象为自行原创/改造的小猪,不得直接使用 Claude(Anthropic)官方吉祥物或任何受版权保护的形象,避免上架 GitHub 后侵权。
素材为原创绘制 或 免费可商用资源;素材文件放 public/pet/。
表情资源:idle(待机)/blink(眨眼)/happy(开心)/sad(难过)/busy(忙碌),至少 5 张图(或 SVG / 单个 Lottie)。
二、功能
2.1 基础形态(本期实现)
固定悬浮于页面右下角(默认),z-index 高于内容但低于关键弹框(费用确认/评价等弹框不被遮挡)。
待机时:轻微上下浮动(CSS 动画)+ 随机间隔眨眼。
点击小猪 → 头顶冒气泡,随机说一句台词(台词写死,见 2.3),数秒后自动消失。
可拖动:用户可拖到任意位置,位置记忆。
可隐藏/显示:提供"收起宠物"按钮;隐藏后在角落留一个小图标可重新唤出;显示/隐藏与位置状态持久化。
2.2 状态联动(本期实现,轻量)
监听平台全局运行状态,自动切表情 + 说对应台词:

全屏
复制
平台状态	宠物表情	示例台词(随机库取一句)
空闲/待机	idle	"今天想测哪个模型呀~"
跑批/试运行进行中	busy	"努力调用中,哼哧哼哧…"
Agent 自动接入中	busy	"正在帮你接口子,稍等哦~"
出现错误/失败	sad	"呜…好像哪里不太对劲"
跑批/接入/评价成功完成	happy	"搞定啦!🎉"
评价完成	happy	"点评写好咯,快看看~"
状态切换有简单过渡;同一时刻只显示一个表情。
联动仅"读"平台状态,不写、不影响任何业务数据。
2.3 台词库
台词写死在 config/petLines.ts,按状态分组,每组若干句随机抽取。
中文为主,可带 emoji;语气轻松卖萌。
2.4 可选彩蛋(本期可不做,标记为后续)
「和小猪聊天」按钮:点开后真调 DeepSeek(复用 Agent 模型 key),用一段"话痨卖萌小猪"性格 prompt 回话。
默认关闭;明确提示"开启后聊天会消耗 token";本地工具中避免干扰干活,做成独立可关面板。
本期可只留入口或完全不做,放 backlog。
三、数据 / 存储
不进 Project / 不进 SCHEMA;宠物偏好单独存 IndexedDB(或 localStorage)一个小键:
typescript
interface PetPrefs {
  visible: boolean;            // 是否显示,默认 true
  position?: { x: number; y: number };  // 拖动后的位置
  chatEnabled?: boolean;       // 是否开启真聊天彩蛋,默认 false
}
刷新后恢复显示/隐藏与位置。
四、前端实现
新增组件 <PetPig />,挂在全局布局(所有板块都能看到)。
状态机:idle / blink / happy / sad / busy,默认 idle,随机眨眼。
监听全局运行事件(跑批/试运行/Agent接入/评价 的 进行中/成功/失败)切换表情;事件可复用现有任务/接入状态。
实现:CSS 动画(浮动/眨眼)+ 切图(或 Lottie);点击冒气泡随机台词;可拖动(指针事件)+ 可隐藏(状态持久化)。
性能:动画不可吃重 CPU;大批量跑批时宠物动画自动降频或保持轻量,不影响主流程。
不遮挡:关键弹框出现时宠物 z-index 低于弹框;不挡住按钮/表格的可点区域。
五、配置
config/petLines.ts(新增):各状态台词库。
public/pet/(新增):原创小猪表情素材(idle/blink/happy/sad/busy)。
(可选)若做聊天彩蛋:复用 DeepSeek key + 一段宠物性格 system prompt。
六、验收
右下角出现原创小猪,待机浮动 + 随机眨眼。
点击 → 冒气泡随机台词 → 数秒消失。
跑批/接入/评价 进行中→busy、成功→happy、失败→sad,表情与台词正确联动。
可拖动到任意位置;可收起,收起后能重新唤出;位置/显隐刷新后保持。
关键弹框(费用确认/评价等)不被宠物遮挡;宠物不挡核心操作。
宠物不影响任何核心数据与流程;关闭宠物后平台一切照常。
大批量跑批时宠物不造成明显卡顿。
(若做聊天彩蛋)默认关闭、有 token 消耗提示、可独立关闭。
七、明确不做
不使用 Claude/Anthropic 官方吉祥物或任何受版权保护形象(原创)。
宠物不参与/不影响 数据存储、接入、跑批结果、评价结果。
复杂养成系统(喂养/等级/经验等)本期不做。
真聊天彩蛋本期可选/可缓做,默认关闭。
八、实施
单独一小批,独立于核心三批,可随时插入(纯前端、低风险)。
顺序建议:先素材原创 → <PetPig /> 基础形态(浮动/眨眼/点击台词/拖动/隐藏)→ 状态联动 → (可选)聊天彩蛋。